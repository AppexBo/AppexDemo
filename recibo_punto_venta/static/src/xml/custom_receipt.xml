<templates id="new_pos_invoice_appexbo" name="point_of_sale.template">
    <t t-name="OrderReceipt" t-inherit="point_of_sale.OrderReceipt" t-inherit-mode="extension" owl="1">
        <!-- Añadir algo al final del recibo -->
        <xpath expr="//div[hasclass('paymentlines')]" position="replace">
           
        </xpath>
        <xpath expr="//div[hasclass('pos-receipt-amount') and hasclass('receipt-change')]" position="replace">
            <!-- Reemplazamos con un div vacío o simplemente no ponemos nada -->
        </xpath>
        <xpath expr="//div[hasclass('pos-receipt-taxes')]" position="replace">
            <!-- Reemplazamos con un div vacío o simplemente no ponemos nada -->
        </xpath>
        <xpath expr="//div[hasclass('pos-receipt-right-align')]" position="replace">
            <!-- Reemplazamos con un div vacío o simplemente no ponemos nada -->
        </xpath>
        <!-- Añadir clase al div principal -->
        <!-- <xpath expr="//div[hasclass('pos-receipt')]" position="attributes">
            <attribute name="class">pos-receipt m-3 my-receipt-style</attribute>
        </xpath> -->
        <!-- Añadir estilo directamente dentro del div del recibo -->
        <!-- <xpath expr="//div[hasclass('pos-receipt')]" position="inside">
            <style>
                @media print {
                    .my-receipt-style {
                        font-size: 24px !important;
                        line-height: 1.8 !important;
                    }
                    .my-receipt-style .receipt-line {
                        padding: 6px 0 !important;
                    }
                }
            </style>
        </xpath> -->
        <xpath expr="//div[contains(@class, 'pos-receipt')]" position="attributes">
            <attribute name="style">margin-top: 0px !important;</attribute>
        </xpath>
        
    </t>
</templates>